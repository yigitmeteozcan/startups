name: Update dataset

on:
  schedule:
    - cron: '17 6 * * *' # daily at 06:17 UTC
  workflow_dispatch: {} # allow manual runs

permissions:
  contents: write

concurrency:
  group: update-data
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --omit=optional

      - name: Install Playwright Chromium (for Antler)
        run: npx playwright install --with-deps chromium

      - name: Fetch latest portfolio data (Techstars + YC + Antler)
        env:
          TYPESENSE_API_KEY: ${{ secrets.TYPESENSE_API_KEY }}
        run: npm run fetch:data

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data STATS.md README.md
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: update startup portfolios dataset ($(date -u +%Y-%m-%d))"
            # Rebase onto any concurrent pushes and retry, so the data commit
            # isn't lost to a race with another push.
            for attempt in 1 2 3 4 5; do
              if git push; then
                echo "Pushed on attempt $attempt."
                break
              fi
              echo "Push rejected (attempt $attempt) — rebasing on origin/main and retrying..."
              git pull --rebase --autostash origin main || exit 1
              sleep 3
            done
          fi
